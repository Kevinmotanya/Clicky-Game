import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1> Clicky Game!</h1>
        <p>Click on an image to earn points, click no more than once!</p>
    </div>
);

export default Title;
